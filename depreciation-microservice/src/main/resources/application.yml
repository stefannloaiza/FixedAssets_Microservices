server:
  port: 8085

spring:
  application:
    name: depreciation-msvc
  datasource:
    driver-class-name: org.postgresql.Driver
    url: jdbc:postgresql://localhost:5435/fixed_assets
    username: postgres
    password: stefannloaiza
  jpa:
    hibernate:
      ddl-auto: update
    database: postgresql